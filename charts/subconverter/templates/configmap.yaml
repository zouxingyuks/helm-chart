{{- if eq .Values.configMode "configmap" }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "subconverter.fullname" . }}-config
  labels:
    {{- include "subconverter.labels" . | nindent 4 }}
data:
{{- range $key, $value := .Values.configFiles }}
  {{ $key }}: |
{{ $value | nindent 4 }}
{{- end }}
{{- end }}
