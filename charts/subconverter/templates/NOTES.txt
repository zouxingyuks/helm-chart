Thank you for installing {{ .Chart.Name }}!

Your release is named {{ .Release.Name }}.

To learn more about the release, try:

  $ helm status {{ .Release.Name }}
  $ helm get all {{ .Release.Name }}

{{- if .Values.ingress.enabled }}

{{- else }}
{{- if .Values.frontend.enabled }}
  You can forward the frontend service to access the web UI:

  kubectl port-forward --namespace {{ .Release.Namespace }} svc/{{ include "subconverter.fullname" . }} 8080:80

  Then access the web UI at: http://localhost:8080

  For direct backend API access:

  kubectl port-forward --namespace {{ .Release.Namespace }} svc/{{ include "subconverter.fullname" . }} 25501:25500

  Then access the backend API at: http://localhost:25501
{{- else }}
  You can forward the service port to your local machine:

  kubectl port-forward --namespace {{ .Release.Namespace }} svc/{{ include "subconverter.fullname" . }} 25500:25500

  Then access the service at: http://localhost:25500
{{- end }}
{{- end }}

Configuration Mode: {{ .Values.configMode }}

To check the health of your deployment:

  kubectl exec -it -n {{ .Release.Namespace }} <pod-name> -- curl http://localhost:25500/version

Useful commands:

  # View logs
  kubectl logs -n {{ .Release.Namespace }} -l "app.kubernetes.io/name={{ include "subconverter.name" . }},app.kubernetes.io/instance={{ .Release.Name }}"

  # View pod status
  kubectl get pods -n {{ .Release.Namespace }} -l "app.kubernetes.io/name={{ include "subconverter.name" . }},app.kubernetes.io/instance={{ .Release.Name }}"

  # Describe the deployment
  kubectl describe deployment -n {{ .Release.Namespace }} {{ include "subconverter.fullname" . }}

Upgrade the release:

  $ helm upgrade {{ .Release.Name }} .

Uninstall the release:

  $ helm uninstall {{ .Release.Name }}
