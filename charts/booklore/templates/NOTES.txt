感谢安装 {{ .Chart.Name }}!

您的 BookLore 数字图书馆已成功部署!

{{- if .Values.mariadb.enabled }}
{{- if eq .Values.mariadb.auth.password "change-me" }}

⚠️  警告: 您正在使用默认密码 "change-me"!
⚠️  这是严重的安全风险,请立即修改密码!

{{- end }}
{{- end }}

1. 获取应用访问 URL:

   {{- if .Values.ingress.enabled }}

   访问地址: http{{ if .Values.ingress.tls }}s{{ end }}://{{ .Values.ingress.hostname }}

   {{- else if contains "NodePort" .Values.service.type }}

   export NODE_PORT=$(kubectl get --namespace {{ .Release.Namespace }} -o jsonpath="{.spec.ports[0].nodePort}" services {{ include "booklore.fullname" . }})
   export NODE_IP=$(kubectl get nodes --namespace {{ .Release.Namespace }} -o jsonpath="{.items[0].status.addresses[0].address}")
   echo http://$NODE_IP:$NODE_PORT

   {{- else if contains "LoadBalancer" .Values.service.type }}

   export SERVICE_IP=$(kubectl get svc --namespace {{ .Release.Namespace }} {{ include "booklore.fullname" . }} --template "{{"{{ range (index .status.loadBalancer.ingress 0) }}{{.}}{{ end }}"}}")
   echo http://$SERVICE_IP:{{ .Values.service.port }}

   {{- else if contains "ClusterIP" .Values.service.type }}

   export POD_NAME=$(kubectl get pods --namespace {{ .Release.Namespace }} -l "app.kubernetes.io/name={{ include "booklore.name" . }},app.kubernetes.io/instance={{ .Release.Name }}" -o jsonpath="{.items[0].metadata.name}")
   echo "访问 BookLore UI,请运行端口转发:"
   echo "  kubectl port-forward --namespace {{ .Release.Namespace }} $POD_NAME 6060:6060"
   echo "然后在浏览器打开: http://localhost:6060"

   {{- end }}

2. 默认管理员凭据:

   {{- if .Values.mariadb.enabled }}

   首次访问时需要创建管理员账户。

   {{- else }}

   首次访问时需要创建管理员账户。

   {{- end }}

3. 查看日志:

   kubectl logs --namespace {{ .Release.Namespace }} -l "app.kubernetes.io/name={{ include "booklore.name" . }},app.kubernetes.io/instance={{ .Release.Name }}"

4. 查看状态:

   kubectl get pods --namespace {{ .Release.Namespace }} -l "app.kubernetes.io/name={{ include "booklore.name" . }},app.kubernetes.io/instance={{ .Release.Name }}"

更多信息请访问: https://booklore.org/docs
